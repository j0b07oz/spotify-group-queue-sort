import {NextRequest,NextResponse} from 'next/server'; import {randomUUID} from 'node:crypto'; import {z} from 'zod'; import {roomByCode,queue,saveTrack} from '@/lib/rooms'; import {mix} from '@/lib/queue'; import {db} from '@/lib/db'; import {syncRoom} from '@/lib/sync'; import {roomToken,spotify} from '@/lib/spotify';
const Submission=z.object({track:z.object({id:z.string().regex(/^[A-Za-z0-9]{22}$/)}),guest_name:z.string().trim().min(1).max(30)});
export async function POST(req:NextRequest,{params}:{params:Promise<{code:string}>}){const r=roomByCode((await params).code);if(!r)return NextResponse.json({error:'Room not found'},{status:404});if(r.locked)return NextResponse.json({error:'The host locked this queue'},{status:423});try{const {track:candidate,guest_name:name}=Submission.parse(await req.json());const live=await spotify(`/tracks/${encodeURIComponent(candidate.id)}`,await roomToken(r));if(!live?.id||live.type!=='track'||live.is_playable===false)throw new Error('This track is not available on Spotify');const t={id:live.id,uri:live.uri,name:live.name,artists:live.artists.map((a:any)=>a.name),artist_ids:live.artists.map((a:any)=>a.id),album:live.album.name,image_url:live.album.images?.[1]?.url||live.album.images?.[0]?.url||null,release_year:Number(live.album.release_date?.slice(0,4))||null,duration_ms:live.duration_ms,genres:[],danceability:null,acousticness:null,energy:null,tempo:null,key_num:null,mode:null,time_signature:null,valence:null};saveTrack(t);db.prepare('INSERT INTO submissions(id,room_id,track_id,guest_name,position) VALUES(?,?,?,?,?)').run(randomUUID(),r.id,t.id,name,queue(r.id).length);const ordered=mix(queue(r.id));db.transaction(()=>ordered.forEach(x=>db.prepare('UPDATE submissions SET position=? WHERE id=?').run(x.position,x.submission_id)))();await syncRoom(r).catch(()=>{});return NextResponse.json({ok:true})}catch(e:any){if(e?.code==='SQLITE_CONSTRAINT_UNIQUE')return NextResponse.json({error:'That track is already in the queue'},{status:409});if(e instanceof z.ZodError)return NextResponse.json({error:'Invalid track or guest name'},{status:400});return NextResponse.json({error:e instanceof Error?e.message:'Invalid submission'},{status:400})}}
