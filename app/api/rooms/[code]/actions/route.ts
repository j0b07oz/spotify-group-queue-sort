import {NextRequest,NextResponse} from 'next/server'; import {roomByCode,queue} from '@/lib/rooms'; import {db} from '@/lib/db'; import {mix} from '@/lib/queue'; import {syncRoom} from '@/lib/sync';
export async function POST(req:NextRequest,{params}:{params:Promise<{code:string}>}){const {code}=await params,r=roomByCode(code);if(!r)return NextResponse.json({error:'Not found'},{status:404});if(req.cookies.get(`host_${code}`)?.value!==r.host_secret)return NextResponse.json({error:'Host access required'},{status:403});const b=await req.json();try{if(b.action==='remove')db.prepare('DELETE FROM submissions WHERE id=? AND room_id=?').run(b.id,r.id);else if(b.action==='move'){const items=queue(r.id),from=items.findIndex(x=>x.submission_id===b.id),to=Math.max(0,Math.min(items.length-1,Number(b.position)));if(from<0)throw new Error('Track not found');const [item]=items.splice(from,1);items.splice(to,0,item);db.transaction(()=>items.forEach((x,i)=>db.prepare('UPDATE submissions SET position=?,pinned=? WHERE id=?').run(i,x.submission_id===b.id?1:x.pinned?1:0,x.submission_id)))()}else if(b.action==='sort'){const items=mix(queue(r.id).map(x=>({...x,pinned:false})));db.transaction(()=>items.forEach(x=>db.prepare('UPDATE submissions SET position=?,pinned=0 WHERE id=?').run(x.position,x.submission_id)))()}else if(b.action!=='sync')throw new Error('Unknown action');await syncRoom(r);return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Action failed'},{status:502})}}
